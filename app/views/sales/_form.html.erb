<%= form_for(@sale) do |f| %>
    <%= f.hidden_field :person_id %>
  <% if @sale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sale.errors.count, "error") %> prohibited this sale from being saved:</h2>

      <ul>
      <% @sale.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
  <% if @sale.person_id %>
    <h3>Buyer: <%= name %></h3>
    <p><%= link_to "(Change user)", people_path %></p>
  <% else %>
    <h3>Choose a buyer</h3>
    <%= select(:sale, :person_id, (options_from_collection_for_select(@people, :id, :name_by_last ))) %> 
  <% end %>
  </div>
  <div class="field">
    <%= f.label :buyer_number %>
    <%= f.text_field :buyer_number %>
  </div>
  <div class="field">
    <%= f.label :sale_date %>
    <%= f.date_select :sale_date %>
  </div>
  <div class="actions">
    <%= f.submit :class => 'btn btn-primary' %>
  </div>
<% end %>
